**From**: DJ (via Jarvis)
**Priority**: high
**Task**: Change assistant name from "Jarvis" to "Andy"

---

## What to do

1. Update `.env` — change `ASSISTANT_NAME=Jarvis` to `ASSISTANT_NAME=Andy` (the default in config.ts is already 'Andy', so the env override just needs removing or updating)

2. Do a codebase sweep for hardcoded "Jarvis" references that should be renamed:
   - Function names like `initJarvisBot`, `sendJarvisMessage`, `sendJarvisPhoto`, `sendJarvisVideo`, `editJarvisMessage`, `deleteJarvisMessage`, `pinJarvisMessage`, `unpinJarvisMessage` — these are internal names and can be renamed for consistency (or left as-is if the refactor risk isn't worth it right now)
   - `JARVIS_BOT_TOKEN` env var — may want to alias or keep for backwards compat
   - Comments referencing "Jarvis" (e.g. `// (nojar) tag — force Jarvis to...`, `// Talking ABOUT, not TO`)
   - `src/translation.ts`: "Users can ask Jarvis directly..."

3. The `(nojar)` tag in comments/engagement logic — decide if this should stay or become `(noandy)` / `(noai)` for consistency

## Notes

- `config.ts` already defaults to `'Andy'` if no env var is set — so the `.env` change is the minimum needed to get the name showing correctly
- The function renames are cosmetic/tech debt and lower priority
- Make sure to restart the bot after `.env` change

